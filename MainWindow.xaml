<Window x:Class="Code2Word.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Code2Word"
        mc:Ignorable="d"
        Title="Code2Word" Height="600" Width="1200"
        Background="#1E1E1E" Foreground="#FFFFFF">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="3" Margin="0,0,0,10">
            <CheckBox x:Name="chkDarkMode" Content="夜晚模式" IsChecked="True" Checked="ChkDarkMode_CheckedChanged" Unchecked="ChkDarkMode_CheckedChanged" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="White" FontSize="14"/>
            <CheckBox x:Name="chkShowLineNumbers" Content="显示行号" IsChecked="True" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="White" FontSize="14"/>
            <CheckBox x:Name="chkRemoveEmptyLines" Content="自动消除空行" IsChecked="True" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="White" FontSize="14"/>
            <CheckBox x:Name="chkAddShading" Content="隔行底纹" IsChecked="False" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="White" FontSize="14"/>
            <Button Content="生成格式化代码" Width="120" Height="30" Click="BtnConvert_Click" Background="#3F3F46" Foreground="White" BorderBrush="#555555"/>
            <StackPanel Orientation="Horizontal" Margin="100,0,0,0" >
                <TextBlock x:Name="txtBorderColor" Text="色条颜色:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="White" FontSize="14"/>
                <Button Width="20" Height="20" Background="CornflowerBlue" Margin="0,0,5,0" Click="ColorSelected_Click"/>
                <Button Width="20" Height="20" Background="#28A745" Margin="0,0,5,0" Click="ColorSelected_Click"/>
                <Button Width="20" Height="20" Background="#DC3545" Margin="0,0,5,0" Click="ColorSelected_Click"/>
                <Button Width="20" Height="20" Background="#FFC107" Margin="0,0,5,0" Click="ColorSelected_Click"/>
                <Button Width="20" Height="20" Background="#6F42C1" Margin="0,0,5,0" Click="ColorSelected_Click"/>
                <TextBox x:Name="txtCustomColor" Width="60" Height="20" Margin="5,0,0,0" Text="#6495ED" VerticalContentAlignment="Center" Padding="2,0"/>
                <Button Content="应用" Width="40" Height="20" Margin="5,0,0,0" Click="CustomColor_Click" Background="#3F3F46" Foreground="White" BorderBrush="#555555"/>
            </StackPanel>
            <Button Content="一键复制" Width="100" Height="30" Click="BtnCopy_Click" Background="#3F3F46" Foreground="White" BorderBrush="#555555" Margin="50,0,0,0"/>
        </StackPanel>
        

        <TextBox x:Name="txtInput" Grid.Row="1" Grid.Column="0" 
                 AcceptsReturn="True" AcceptsTab="True" 
                 HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="14"
                 Background="#232122" Foreground="#D4D4D4"
                 BorderBrush="#3F3F46" BorderThickness="1" Padding="5"/>

        <GridSplitter Grid.Row="1" Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#3F3F46" Margin="5,0"/>

        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="104*"/>
                <ColumnDefinition Width="479*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <RichTextBox x:Name="rtbOutput" Grid.Row="1" 
                         HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="14"
                         Background="#232122" Foreground="#D4D4D4"
                         BorderBrush="#3F3F46" BorderThickness="1" Padding="5" Grid.ColumnSpan="2">
                <FlowDocument>
                </FlowDocument>
            </RichTextBox>
        </Grid>

        <Border x:Name="toastNotification" Grid.Row="1" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,50" 
                Background="#28A745" CornerRadius="5" Padding="20,10" Opacity="0" IsHitTestVisible="False">
            <TextBlock Text="已复制到剪贴板！" Foreground="White" FontSize="16" FontWeight="Bold"/>
        </Border>
    </Grid>
</Window>
